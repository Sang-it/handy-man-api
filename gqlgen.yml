schema:
- ./schemas/*.graphql

exec:
  filename: generated/exec.go

model:
  filename: graphql-types/generated.go

resolver:
  layout: follow-schema
  dir: resolvers
  package: resolvers
  filename_template: "{name}.resolvers.go"
